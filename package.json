{
  "name": "",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html && npm run copy",
    "dev": "node app.js & npm run build && parcel index.html",
    "start": "npm run build",
    "copy": "node copy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "express": "^4.17.1",
    "fs-extra": "^9.1.0",
    "parcel-bundler": "^1.12.4"
  }
}
